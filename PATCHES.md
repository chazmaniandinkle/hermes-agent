# Myrgic Local Patch Registry

**Canonical, in-repo registry of every local modification to this vendored checkout of `NousResearch/hermes-agent`.**
Lives on the `local` branch. Read before editing any file under "Files to Watch."

> This file is the single source of truth for *what's patched*. The external journals
> `~/.hermes/journals/hermes-upstream-activity.md` and `hermes-upstream-drift.md` are
> append-only time-series logs maintained by the `hermes-upstream-reconciler` cron.

## The vendoring discipline

- **Priority order for any change:** (1) upstream via PR → (2) user-space plugin/hook in
  `~/.hermes/` (survives `git reset`) → (3) a tracked patch in this repo (last resort, at-risk of
  rebase churn). Keep this checkout as close to pristine upstream as possible.
- **Branch model:** `main` = pristine mirror of `origin/main`, never commit here, only
  `git fetch && git reset --hard origin/main`. `local` = `origin/main` + the patches below + this
  file — **the gateway runs from `local`.**
- **Sync = rebase, not reset:** `git fetch origin && git rebase origin/main local`. A patch whose
  upstream PR merges drops out automatically (commit goes empty); conflicts surface exactly on the
  patches that need updating.
- **When a patch is born:** register it here → classify it (upstream-track / local-permanent /
  deliberate-divergence) → decide the track using the priority order above → file/comment/keep per
  that track's own state machine (see sections below).
- **Resync cadence:** ad hoc, triggered by drift audits or triage sessions (most recent:
  2026-06-10, 2026-07-04) — not yet on a fixed schedule. Each resync re-verifies every open
  upstream-track item and re-runs the affected test suites.
- **Identity rule:** PATCH-xxx / EXT-xxx numbers are identity-referenced and **never reused or
  renumbered** once assigned, even if the underlying commit is dropped, superseded, or the number
  briefly collided with other same-day work (see PATCH-013/014's numbering note below). Categorize
  and re-categorize freely via sections and metadata; the ID itself is permanent.

---

## ⚙️ Gateway processes on this node — manage ALL of them

This node runs **one gateway process per agent profile**, each a separate launchd job with its own bot tokens/config, but **all sharing this single checkout** (one working tree, no worktrees → **the branch is node-global**):

| launchd job | profile | bots | config | logs |
|-------------|---------|------|--------|------|
| `ai.hermes.gateway-cog` | `--profile cog` | Telegram **Cog** (`cogos_cog_bot`) + Discord **Cog#5887** | `~/.hermes/profiles/cog/.env` | `~/.hermes/profiles/cog/logs/` |
| `ai.hermes.gateway-darkstar` | `--profile darkstar` | Telegram (`cogos_darkstar_bot`) + Discord **Darkstar#0987** | `~/.hermes/profiles/darkstar/.env` | `~/.hermes/profiles/darkstar/logs/` |

(The old default-profile `ai.hermes.gateway` job was retired 2026-06-08 — its plist survives as `ai.hermes.gateway.plist.retired.20260608`. Bot identities re-verified at the 2026-07-04 cutover restart.)

- **One gateway serves ALL its channels** (Telegram + Discord + …) as adapters in a single process — *not* one gateway per channel. "Gateway running with 2 platform(s)" = both adapters in one process.
- **Code is shared and node-global.** Both jobs have `WorkingDirectory=/Users/slowbro/.hermes/hermes-agent` and run whatever single branch is checked out (`local`). A `git checkout main` / `git reset` flips **both** gateways to unpatched code on their next restart. **The "stay on `local`" discipline is node-wide, not per-profile.**
- **A code change is only live after each process restarts.** Python loads code at process start; flipping the on-disk checkout does NOT affect an already-running gateway. **After any branch change/rebase, restart EVERY gateway** (`launchctl kickstart -k gui/$(id -u)/<job>`) or you'll have one profile patched and another silently running stale code (this exact split happened 2026-06-03: default was restarted onto `local`, cog kept running boot-time `main` code until separately restarted).
- **Enumerate before acting:** `launchctl list | grep hermes` (note PID vs `-`), `ps -o pid,lstart,command -p <pids>` (start time tells you which branch's code a process loaded), and match the bot identity (`getMe`) to the bot in question. Never assume the gateway you found is the only one.

---

## Section index

- [UPSTREAM-TRACK](#upstream-track) — patches whose end state is an upstream PR/comment; four sub-states track exactly where each sits:
  - FILED-OPEN — PR already filed, open, unmerged (PATCH-006, -009, -010, -012)
  - READY-TO-FILE — draft complete, held pending operator go (PATCH-014)
  - NEEDS-WORK-TO-FILE — real candidate, blocked on local work before submission (PATCH-013; PATCH-007+008 joint; EXT-005; PATCH-015)
  - COMMENT-DONT-FILE — real bug, but competing upstream PRs already cover the surface (PATCH-011)
- [LOCAL-PERMANENT](#local-permanent) — site-specific, no generic upstream audience, will never be filed (PATCH-002, -004, EXT-001-Gate1-note, EXT-002/003/004/007/008/009/010)
- [DELIBERATE-DIVERGENCE](#deliberate-divergence) — upstream has a stance (shipped a lighter fix, or has no equivalent by design) that this repo has knowingly chosen not to follow (PATCH-001 + its xfail test-deltas, EXT-001-Gate2)
- [ABSORBED-BY-UPSTREAM](#absorbed-by-upstream) — historical; upstream shipped the equivalent, patch dropped out at rebase (PATCH-003, Gate 1 of `da816abe5`)
- [Files to Watch](#files-to-watch-upstream-changes-here-may-break-a-patch-or-plugin)
- [User-Space Extensions](#user-space-extensions-safe--outside-this-repo-survive-any-reset) index
- [History](#history)

---

## UPSTREAM-TRACK

Patches whose end state is an upstream PR (filed, comment-only, or drafted-and-held). Sub-state
tracks exactly where each one sits in that pipeline.

### FILED-OPEN — PR already filed, open, unmerged

#### PATCH-006: TTS concurrent output path collision (`tools/tts_tool.py`)
- **Status:** LIVE on `local`, replayed clean at the 2026-07-04 resync (single-line change, no conflict).
- **Files:** `tools/tts_tool.py` (1 line)
- **What:** `text_to_speech_tool` named output files `tts_<YYYYMMDD_HHMMSS>.<fmt>` — second-level precision. Concurrent `/api/audio/speak` requests in the same second collided on one path and raced: one unlinked the file mid-write of another → "TTS provider produced no output" errors (observed with the mod3 command provider during desktop voice playback). Fix adds `%f` (microseconds) to the strftime format so concurrent calls get unique paths.
- **Provenance:** Born 2026-06-03 13:18:20 EDT (`08bbab862`), registered same day 13:23:55 (`7958c6e72`). Observed live during concurrent desktop voice playback via the mod3 command provider. Same-session sibling attempt PATCH-005 (voice chunk pipelining) was tried and scrapped — it didn't fix the gap, so this timestamp-width fix is what actually shipped. (Note: a different, unrelated draft briefly held the number "PATCH-005" in the external `kanban-closed-loop-supervisor` skill registry — two independent items briefly shared a number before one was scrapped; no collision with this repo's numbering.)
- **Upstream refs:** **PR #43911 OPEN** (ours), unmerged as of the 2026-07-05 xref. Verified live in `upstream/main:tools/tts_tool.py:2233` — still second-precision `datetime.datetime.now().strftime("%Y%m%d_%H%M%S")`, unchanged. No competing fix from another author found.
- **Test-delta notes:** none — kept-clean through both rebases (1-line, no conflict).
- **Re-merge-risk:** LOW — additive precision on a filename, no behavior change otherwise.
- **Reconciler action:** ping the PR on the next reconciler sweep; drops out at rebase when #43911 merges.

#### PATCH-009: `/s` alias for `/steer` (`ui-tui/src/app/slash/commands/core.ts`)
- **Status:** ON `local`. **Live after `npm run build` in `ui-tui/` + TUI relaunch.**
- **Files:** `ui-tui/src/app/slash/commands/core.ts` (one line: `aliases: ['s']` on the `steer` command).
- **What:** Adds `s` as a short alias for the TUI `/steer` slash command (inject a message after the next tool call without interrupting). Uses the existing `aliases?: string[]` field on `SlashCommand` — `registry.ts` already flat-maps `[cmd.name, ...cmd.aliases]` into the command lookup, so no dispatch change needed.
- **Why:** User convenience — `/steer` is used mid-turn and a single-char alias is faster to type. Cosmetic/ergonomic, not behavioral. Collision check: no existing command or alias named `s` (verified against `core.ts`/`session.ts`). Distinct from the `/q → queue` lesson (#31983): `s` was free.
- **Provenance:** Born 2026-06-04 17:42:53 EDT (`c66d40b5e`). No incident — pure ergonomics.
- **Upstream refs:** **PR #43912 OPEN** (ours), unmerged. Verified live: upstream's `steer` command entry (`ui-tui/src/app/slash/commands/core.ts:599`) still has no `aliases` field — replayed clean, zero conflict. No competing claim found.
- **Test-delta notes:** `slashParity.test.ts` 3/3 pass; `npm run build` clean (2.9mb bundle); alias confirmed compiled into `dist/entry.js`.
- **Re-merge-risk:** trivially re-appliable; drops out at rebase when #43912 merges.

#### PATCH-010: `/restart` ack lost to teardown race (`gateway/platforms/base.py`, `gateway/run.py`)
- **Status:** ON `local`. **Adapted at the 2026-07-04 resync** — upstream independently rewrote `cancel_background_tasks` with a bounded 5s hard-cancel + `MAX_DRAIN_ROUNDS` retry-loop (for late-arriving tasks during drain — a related but distinct problem) and refactored the call sites in `gateway/run.py` into a shared `_bounded_adapter_teardown(adapter, platform, *, profile=None)` helper. Adaptation: `grace_seconds` param added to both `cancel_background_tasks` and to `_bounded_adapter_teardown` itself. **Live after each gateway restart.**
- **Files:** `gateway/platforms/base.py` (`cancel_background_tasks` gains `grace_seconds=0.0`), `gateway/run.py` (`_bounded_adapter_teardown` gains `grace_seconds=0.0` and forwards it; both call sites in `_stop_impl` pass `grace_seconds=2.0 if self._restart_requested else 0.0`), `tests/gateway/test_restart_ack_grace.py` (3 tests).
- **What:** The `/restart` handler queues its ack ("♻ Restarting gateway...") and `request_restart` begins `stop()` 50ms later. With no active agents the drain phase is instant, and `cancel_background_tasks()` cancelled the in-flight `_process_message_background` task mid-HTTPS-send — `CancelledError` propagates silently (no failure log, no retry), so the ack never reached the chat. Observed 3/3 on 2026-06-10 (20:46, 21:38, 21:39). Fix gives in-flight tasks a bounded grace window (2s, restart path only) to finish naturally before cancellation; plain stops are unchanged. The ack's text is the recovery instruction for the PATCH-003 failure mode — the race ate the one message telling the operator what to do when the gateway bricks.
- **Provenance:** First landed 2026-06-10 21:51:43 EDT (`55477be7b`). Found while verifying PATCH-003's launchd re-application that same evening. Root cause reproduced 3/3.
- **Upstream refs:** **PR #43903 OPEN** (ours), unmerged. PR body cross-refs **#8008** (same race class for Discord's untracked `_pending_text_batch_tasks`) and **#36913** (bounding shutdown awaits) as related-but-non-overlapping open work — confirms this is a genuinely separate, still-open problem space. No new competing PR found since resync.
- **Test-delta notes:** all 3 `test_restart_ack_grace.py` tests pass against the rebased `base.py` body. **TODO(hygiene):** re-merge gap in `test_bounded_adapter_teardown.py` — 2 tests using zero-arg mocks `TypeError`'d on the new `grace_seconds` kwarg (silently swallowed by a broad `except` in the teardown helper); fixed forward 2026-07-04 12:05:55 EDT (`8ce044176`, "only forward grace_seconds when non-default") — a genuine production-code fix, not just a test patch. Closed as of this writing; flagged here as the historical hygiene item the shareability audit called out.
- **Re-merge-risk:** LOW — additive default-off parameter; only the planned-restart path changes timing (≤2s slower teardown when a send is in flight).
- **Reconciler action:** watch `cancel_background_tasks` and `_bounded_adapter_teardown` for further churn; drop if upstream adds an equivalent flush-before-cancel.

#### PATCH-012: cron inactivity-timeout teardown races the orphaned worker (`cron/scheduler.py`)
- **Status:** ON `local`. **Rebased 2026-07-04**: conflict was a pure positional collision, not functional overlap — upstream independently added an unrelated `_guard_job_credential_exfil` security-guard function at the same insertion point where PATCH-012's `_teardown_cron_job_resources` was inserted. Resolved as sibling module-level functions; rest of the diff auto-merged clean. **Live after each gateway restart** (editable install — `cron/scheduler.py` imports straight from this checkout).
- **Files:** `cron/scheduler.py` (+172/−39): new module-level `_teardown_cron_job_resources` helper; `_cron_future = None` pre-bind before the job `try:`; `process_registry.kill_all(task_id=_cron_session_id)` added to the inactivity-timeout branch; `finally:` session-db/agent teardown rewired through the helper.
- **What:** After an inactivity timeout, `_run_job_impl`'s `finally:` closed `_session_db` and called `agent.close()` while the timed-out job's worker thread was still executing — `ThreadPoolExecutor.shutdown(wait=False, cancel_futures=True)` cannot stop a running future. `agent.close()`'s `kill_all` then unblocked the stuck tool, and the resumed worker's `_flush_messages_to_session_db` hit the closed connection, silently dropping the turn's rows. Fix: the timeout branch does the subprocess kill itself (keeps the unblock behavior); teardown ownership transfers to the future via `add_done_callback` (close runs on the worker thread after its final writes land); a daemon `Timer` (`HERMES_CRON_TEARDOWN_HARDCAP`, default 900s) bounds the deferral so a worker that never finishes cannot leak the SQLite handle/subprocesses/sockets forever.
- **Why:** Root cause of the PATCH-011 incident class (darkstar `errors.log:2652-2660`, job `hermes-upstream-reconciler`, session `cron_b02d262b332d_20260703_060318`, 2026-07-03 06:21:47; TRIAGE issue 2). PATCH-011 typed the failure; PATCH-012 stops the message loss.
- **Provenance:** Born 2026-07-04 10:05:45 EDT (`d91419ff9`); PR filed same day 10:13:36 EDT (`2f2439977`). Same incident as PATCH-011 (root-cause companions from the same 2026-07-04 triage pass). A boolean-skip sketch was proposed first (`fixes/inv-persistence.md`) and explicitly self-flagged as "a sketch, not a verified patch" — risked leaking fds/subprocesses if an orphaned future never completed. PATCH-012 supersedes that sketch, validated by a standalone 4-scenario repro harness (`fixes/patch012-repro/`) before commit.
- **Upstream refs:** Filed as **NousResearch/hermes-agent#58311** (branch `fix/cron-teardown-race`, `chazmaniandinkle` fork), commit `b479f229408`, **OPEN**. Prior art cross-referenced in the PR thread: **#54755** (same failure signature) and **#18004** (orphaned-worker premise), both open. **New competitor found in the 2026-07-05 xref, not yet cross-linked on the PR:** **PR #26752** (different contributor, `iamagenius00`, 2026-05-16 — predates #58311). Three mechanism differences worth surfacing to a reviewer: (1) #26752 adds a fixed 5s blocking grace-wait on the scheduler thread before deferred cleanup, vs. our non-blocking `add_done_callback`; (2) #26752 does not move `kill_all` earlier — ours calls it right after `agent.interrupt()` to guarantee the stuck tool actually unblocks; (3) #26752 has no hard-cap timer equivalent to `HERMES_CRON_TEARDOWN_HARDCAP`. **TODO(upstream-comment): cross-link #26752 on #58311** so reviewers don't have to reverse-engineer the 3-way comparison — do not withdraw #58311, it has a safety property #26752 lacks.
- **Test-delta notes:** standalone repro harness (`patch012-repro/`) confirms all 4 scenarios hold on the rebased body; full `tests/cron/` suite (618 tests) passes.
- **Re-merge-risk:** MEDIUM — teardown timing changes on the orphaned-worker path only. Accepted residual: under the hard cap a still-running worker's late write fails typed (via PATCH-011) instead of landing; deferred `end_session` means a timed-out cron session row stays open until its worker exits (more truthful, but a visibility change).
- **Interaction note:** PATCH-011 (`hermes_state.py` guard) is the companion, not superseded — it is what makes the hard-cap residual race fail typed. Keep both.
- **Reconciler action:** watch `cron/scheduler.py`'s `_run_job_impl` (inactivity-timeout branch + `finally:` teardown) and `_teardown_cron_job_resources` for upstream churn; drop at rebase when #58311 (or an equivalent deferred-teardown fix) merges.

### READY-TO-FILE — draft complete, held pending operator go

#### PATCH-014: Idle-triggered background self-improvement review (`agent/background_review.py`, `run_agent.py`, `hermes_cli/config.py`)
- **Status:** ON `local` (this commit). **Live after each gateway restart.**
- **Numbering note:** the 2026-07-04 triage dispatch referred to this work as "PATCH-013" before the same-day resync consumed that number for the OAuth Gate 2 entry (see DELIBERATE-DIVERGENCE below). Per the identity-collision rule (chronologically-first-and-already-drafted wins, then cascade), this work was renumbered **PATCH-014** in the registry; the working-notes files keep their original `patch013-*` filenames (`~/workspaces/cog/.cog/mem/working/2026-07-04-hermes-triage/fixes/patch013-notes.md`) — a deliberate exception to renumbering-cascade since those are working files, not identity-referenced registry entries.
- **Files:** `agent/background_review.py` (+~250: `IdleReviewScheduler`, `_idle_trigger_config`, `get_idle_review_scheduler`, `note_foreground_turn_start/end` — additive section at end of module), `run_agent.py` (`_spawn_background_review` becomes a dispatcher; old body renamed `_spawn_background_review_now`; `run_conversation` forwarder gains turn-start/turn-end activity stamps in a try/finally; `close()` step 0 + `release_clients()` cancel the scheduler), `hermes_cli/config.py` (defaults `auxiliary.background_review.idle_trigger_seconds: 0`, `coalesce: true`), `tests/run_agent/test_background_review_idle_trigger.py` (13 tests, new).
- **What:** New config `auxiliary.background_review.idle_trigger_seconds` (default **0** = stock immediately-post-turn spawn, byte-compatible). When > 0, turn end does NOT start review inference — the review is debounced and fires only once the session has been idle that long, re-checked at fire time against both an active-foreground-turn flag and a last-activity timestamp. `coalesce` (default **true**): N turns completing before an idle window opens produce ONE review from the newest cumulative snapshot with OR-merged trigger flags; `coalesce: false` queues per-turn entries and drains one per idle window. Timer is a daemon `threading.Timer`; `shutdown()` is idempotent — PATCH-012-compatible teardown, no leaked timer at gateway shutdown.
- **Why:** Operator directive 2026-07-04, quoted verbatim in `fixes/patch013-notes.md`: *"It would be nice if it could wait until all of the previous messages had been processed before triggering so that it wouldn't end up queueing."* Concrete incident: cog-profile Discord session `20260704_230542_090ad18d` — the post-turn background-review fork inherited the live conversation and full toolset, queueing ahead of interactive turns on the single-lane LM Studio provider `cog` runs.
- **Provenance:** Dispatched during the 2026-07-04 Hermes triage session, committed 2026-07-05 00:10:40 EDT (`e8bbc3aa1`).
- **Config (this node):** `cog` profile runs `idle_trigger_seconds: 120`; `darkstar` stays at the default 0.
- **Test-delta notes:** new module 13/13; full `tests/run_agent/test_background_review*.py` 48/48; `tests/cron/` 618/618; `tests/test_hermes_state.py` 303/303; config suites 182/182. Live-verified end-to-end same night: a turn deferred a review at 00:19:42, window stayed silent until 00:21:43 (120.1s later), review fired with a single bounded API call.
- **Upstream refs:** **No competing mechanism found anywhere upstream** — zero hits searching `debounce`, `idle_trigger`, `IdleReviewScheduler`, `coalesce` against `upstream/main:agent/background_review.py` (907 lines). Motivating pain strongly attested by 5 open corroborating issues: **#54118 / #54115** (duplicate OOM/slowdown reports, same root class — single local-inference backend shared between main turn and review fork), **#30970** (notification suppression, adjacent), **#42391** (TUI goal-continuation preemption, adjacent), **#39996** (tool-schema thrash against weak local models, adjacent). PR materials drafted and HELD pending operator go (`fixes/patch013-upstream-pr.md`); not yet pushed/filed as of 2026-07-05.
- **Re-merge-risk:** MEDIUM-LOW — default-off (0 = stock path through the same `_spawn_background_review_now` body); enabled path changes only *when* the review starts, never whether the foreground turn runs.
- **Interaction note:** PATCH-012 (`cron/scheduler.py` deferred teardown) is adjacent, not overlapping — cron teardown calls `agent.close()`, which now also cancels this scheduler (idempotent, no new ordering constraint). The review fork sets `_memory_nudge_interval = _skill_nudge_interval = 0`, so a fork never schedules recursively.
- **Reconciler action:** **highest community-value, highest-readiness upstream candidate in this registry** — file first among all open candidates (5 open corroborating issues, zero competing mechanism). Watch `run_agent.py`'s `_spawn_background_review`/`close`/`release_clients`/`run_conversation` forwarder and `agent/background_review.py`'s module tail for upstream churn; drop if upstream adds an equivalent idle/debounce trigger.

### NEEDS-WORK-TO-FILE — real upstream candidate, blocked on local work before submission

#### PATCH-013: OAuth Gate 2 — relocate agent identity out of `system` field (`agent/anthropic_adapter.py`)
- **Status:** ON `local` (newly numbered at the 2026-07-04 resync; previously untracked as a discrete PATCH-ID — it rode inside `da816abe5` alongside the now-upstreamed Gate 1 tool-prefix fix, see ABSORBED-BY-UPSTREAM below).
- **Files:** `agent/anthropic_adapter.py` (`_relocate_identity_into_first_user` helper + the `is_oauth` branch of `build_anthropic_kwargs`).
- **What:** The OAuth system-content classifier routes a request to the metered overage lane when the `system` field carries a large agent identity block (memory/skill/boundaries markers), even with zero tools. Relocates that identity into the first user turn — the way Claude Code injects `CLAUDE.md` — leaving only the canonical Claude Code system string in `system`. Block-order safe: if the first user turn leads with a `tool_result` block, the identity is appended after it.
- **Provenance:** Originally landed 2026-05-30 as one half of `da816abe5` ("complete the dual billing-gate fix on the direct Anthropic path"), alongside the Gate 1 fix. Re-applied onto the rebased tree 2026-07-04 11:26:18 EDT (`93481ddde`) and renumbered PATCH-013 the same day — Gate 1 dropped out because upstream had independently landed the identical fix (see ABSORBED-BY-UPSTREAM); the surviving Gate 2 half needed a fresh patch ID since the original combined commit no longer applies as a unit. Root RCA: `cog://mem/semantic/research/2026-05-29-anthropic-oauth-dual-gate-definitive` (2026-05-29) — establishes that Gate 2 triggers OVERAGE billing regardless of tool names whenever a large agent-identity block sits in `system`, content-semantic (three discrete categories), not size.
- **Verified (2026-07-04):** end-to-end functional check against the rebased body — system field drops from a 50KB+ identity block to the 57-char canonical string; sanitized identity lands correctly in the first user turn; Gate 1's `mcp__` prefixing (already upstream) undisturbed; tool_result block-order safety confirmed at the unit level (full-pipeline test of this specific case is confounded by upstream's own unrelated `_strip_orphaned_tool_blocks` hardening rejecting a synthetic conversation with no preceding user turn — never occurs in a real session).
- **Interaction note:** EXT-001 `oauth_billing_gate` installs its own independent copy of this same relocation logic and fully replaces `build_anthropic_kwargs` with a wrapper, so the fix holds even if this source-level patch is lost to a future reset. This patch and EXT-001 are redundant-but-harmless together.
- **Upstream refs:** This is the **most contested surface in the registry** — an extensive "swarm" of prior upstream attempts. **PR #47738 OPEN** is the closest competitor: same core mechanism (move system content to the first user turn on the OAuth path), but wraps content in an explicit `<system_context>` block where this patch mirrors CC's own `CLAUDE.md`-injection convention instead, and **already demonstrates verified cache-control preservation** (`cache_read=48641` on turn 2, `cache_control: ephemeral` attached to the relocated block) — the one piece this patch's own dossier entry does not document verifying. #47738 handles a leading `tool_result` block worse (its summary doesn't mention it; PATCH-013 does). **PR #13611 OPEN** — older, independent (2026-04-21), uses a `[CONTEXT]...[/CONTEXT]` + synthetic assistant `Understood.` acknowledgment (burns a turn neither PATCH-013 nor #47738 do). **Issue #53212 CLOSED** — best root-cause writeup upstream, but closed as issue-hygiene not fix-landed (the fix PR it references, #47738, is itself still open). **Issue #26430 CLOSED** — credited by #47738 as original relocation approach it built on. #47738 itself supersedes three earlier sibling attempts (#33570 capped-prompt, rejected as lobotomizing; #23361 string-replace whack-a-mole; #20865 env-flag bypass), all still open/unmerged but not competing on the merits per #47738's own analysis.
- **NEEDS-WORK gap:** **cache-control verification.** #47738 already demonstrates reviewers will ask for proof the relocated block preserves prompt caching; this patch's dossier entry does not document that check having been run. **TODO(before filing): verify `cache_control: ephemeral` behavior on the relocated identity block and record the cache-hit evidence**, then file citing #47738 (primary competitor, extend not duplicate), #13611 (older/independent), #53212 (root-cause writeup), #26430 (credited prior art) — see the ranked-priority note below.
- **Re-merge-risk:** the file's `is_oauth` branch and `convert_messages_to_anthropic` (upstream's message-history normalization runs BEFORE this patch's relocation call) are the two watch points for future upstream churn.
- **Reconciler action:** close the cache-control gap, then file as a narrowly-scoped PR; watch `agent/anthropic_adapter.py`'s `build_anthropic_kwargs` `is_oauth` branch for churn in the meantime.

#### PATCH-007 + PATCH-008 (joint): Claude Code OAuth credential is read-only (`agent/credential_pool.py`, `agent/anthropic_adapter.py`)
Filed together — same contract, same companion actuator, one PR narrative.

##### PATCH-007 — `agent/credential_pool.py`
- **Status:** ON `local`. **Rebased 2026-07-04** onto upstream's independently-added `openai-codex`-specific serialization branch (`_refresh_entry` split into a dispatcher + `_refresh_entry_impl`) — real conflict, resolved by placing PATCH-007's `anthropic`/`claude_code` early-return branch in the dispatcher ahead of the codex branch. Verified: all 101 tests across `test_credential_pool.py` + `test_credential_pool_routing.py` + `test_credential_pool_oauth_writethrough.py` pass, 0 regressions. **Live after each gateway restart.**
- **Files:** `agent/credential_pool.py` (`_refresh_entry`, +~30 lines, additive branch at top)
- **Companion (user-space, EXT-010):** `~/.hermes/bin/claude-token-refresh` — the actuator.
- **What:** For `provider=="anthropic" and source=="claude_code"`, Hermes no longer POSTs the credential's single-use OAuth refresh token. Claude Code (CC) is the sole holder/writer of that token (macOS Keychain `Claude Code-credentials`); Hermes POSTing it raced CC and wrote the rotated pair only to `~/.claude/.credentials.json` — a file CC ≥2.1.114 ignores — orphaning the rotation and forcing a manual `claude /login`. New behavior: **mirror** whatever CC currently holds (keychain-first); if even the keychain is stale, **delegate** refresh to the owner via EXT-010, then re-mirror.
- **Provenance:** Born 2026-06-03 17:31:54 EDT (`8b5feba66`). Root-caused live 2026-06-03 — file token `eb374e46…/exp 00:08` vs keychain `922ff595…/exp 00:49`, one refresh-cycle apart. Cross-referenced against the CogOS kernel's own WriteBack fix that shipped the same week (myrgic/cogos#363/#364) — one symptom of a wider file-vs-keychain token-poisoning class the kernel side was independently chasing.
- **Safety / degradation:** any failure in the branch falls through to `_mark_exhausted` → the existing exhausted-entry keychain resync in `_available_entries` recovers on the next selection (no worse than pre-patch). Irreducible floor: a genuinely **revoked** refresh token can't be re-auth'd headlessly → needs interactive `claude /login`.
- **Test-delta notes:** module compiles + imports in venv; unit test (POST sentinel raises if touched) confirms all three paths — fresh / owner-refreshed / revoked — never POST the refresh token.
- **Interaction note:** EXT-001 `oauth_billing_gate` monkeypatches `agent/anthropic_adapter.py` (outbound billing/routing) — orthogonal to this pool-refresh gate, no shared symbol.

##### PATCH-008 — `agent/anthropic_adapter.py`
- **Status:** ON `local`. **Highest-risk re-merge of the 2026-07-04 resync** — manually re-applied onto upstream's own OAuth-credential hardening (`1dde7e2f2`/`5a5396aec`), which added a race-*mitigation* "adopt already-refreshed token" fast path but still falls through to a direct POST + file write when no fresher credential is found. Kept upstream's fast path (strictly better, free); routed the fallback through the EXT-010 actuator instead of upstream's POST. `_write_claude_code_credentials` stays a hard no-op. **Live after each gateway restart.**
- **Files:** `agent/anthropic_adapter.py` (`_refresh_oauth_token` → read-only actuator delegation; `_write_claude_code_credentials` → no-op). Tests: `tests/agent/test_anthropic_adapter.py` (`TestRefreshOauthToken`, `TestWriteClaudeCodeCredentials`), `tests/agent/test_auxiliary_client.py` (force-refresh test rewritten to assert the read-only contract).
- **Companion:** EXT-010 actuator `~/.hermes/bin/claude-token-refresh` (shared with PATCH-007).
- **What:** Completes PATCH-007. PATCH-007 made `credential_pool._refresh_entry` read-only but left a second, uncovered refresh path: `resolve_anthropic_token()` → `_resolve_claude_code_token_from_credentials()` → `_refresh_oauth_token()`, reachable from agent init / usage polls / `_refresh_provider_credentials`. That path still POSTed the single-use refresh token and wrote `~/.claude/.credentials.json`. Now `_refresh_oauth_token` delegates to the owner via EXT-010 then re-reads keychain-first (no POST, no write); `_write_claude_code_credentials` is a hard no-op (belt-and-suspenders).
- **Provenance:** Born 2026-06-04 (registry-listed as landing the day after PATCH-007). Observed live 2026-06-04, *after* the CogOS kernel WriteBack fix had already eliminated the dominant ~10-second poisoning loop — one residual path still POSTed and still wrote the file on a single stale-write, still forcing `/login`. Same root shape as PATCH-007, found by testing PATCH-007's own fix under load.
- **Test-delta notes:** module compiles + imports in venv; 7 unit tests pass in `TestRefreshOauthToken`/`TestWriteClaudeCodeCredentials` (POST/write sentinels raise `AssertionError` if touched); full `test_anthropic_adapter.py` + `test_auxiliary_client.py` re-run = exactly the same 14 pre-existing failures as plain `origin/main`, 0 new failures. **TODO(hygiene, blocks filing): `test_anthropic_keychain.py::test_falls_back_to_network_refresh_when_no_fresh_credential`** was marked `xfail` on 2026-07-04 12:07:27 EDT (`8ae25def2`) rather than rewritten to assert the actuator-delegation contract — it still asserts the *superseded* direct-POST contract. A reviewer reading the test suite for a future upstream PR would see it contradicting the PR's own claim of a strict never-POST invariant. This must be fixed (test rewritten to assert delegation, not xfail'd) before PATCH-007/008 are filed jointly.
- **Interaction note:** EXT-001 `oauth_billing_gate` monkeypatches this same file (outbound billing/routing) — orthogonal to these two credential functions, no shared symbol; re-verified 2026-07-04, both gates apply correctly through the patched `build_anthropic_kwargs`.

**Joint upstream refs (PATCH-007 + PATCH-008):** `1dde7e2f2` ("adopt Claude Code's already-refreshed token before racing refresh") and `5a5396aec` ("reconcile keychain/file credentials when one is expired") are both real, already-landed upstream commits — weaker versions of the same idea (they *reconcile/adopt* a fresher token opportunistically but do not carry the strict never-POST invariant). **PR #40107** ("fall back to credential file when Claude Code keychain token is expired") — same contributor area, CLOSED, not merged (a third, unmerged attempt). No new PR since resync proposing the strict read-only contract. A future PR extending this invariant would be additive to `1dde7e2f2`/`5a5396aec`, not duplicative — frame it that way, citing both by hash.

**Joint reconciler action:** fix PATCH-008's stale keychain test first, then file one PR covering both patches as a single strict-read-only-contract change.

#### EXT-005: `skill_relevance_gate` plugin — pending site-specificity check
- **Location:** `~/.hermes/plugins/skill_relevance_gate/` (user-space, safe from `git reset`).
- **What:** Filters skill *loading* by relevance to the current task, to reduce irrelevant skill-context injection.
- **Provenance:** Predates the 2026-06-03 registry (its test file, `tests/plugins/test_skill_relevance_gate.py`, is one of the two items tracked as PATCH-002's untracked residue). No incident record recovered.
- **Upstream refs:** **No upstream plugin/issue/PR at the loading stage.** Issue **#50988 OPEN** is adjacent, not competing: an LLM-judged post-load pruning pass inside the existing `background_review` skill-nudge fork — different pipeline stage (post-load pruning vs. pre-load filtering), same underlying goal. A future PR for this concept would be complementary to #50988, not duplicative.
- **NEEDS-WORK gap:** the implementation itself has never been checked for site-specific assumptions (Myrgic-local paths, naming, config shape baked in). **TODO(before filing): audit `skill_relevance_gate`'s implementation for hardcoded Myrgic assumptions** before treating this as a clean, portable upstream candidate.
- **Re-merge-risk:** N/A — user-space plugin, outside the tracked repo.
- **Reconciler action:** second-highest-value genuinely-novel candidate in the registry after PATCH-014, but lower urgency (no open issue demanding it) — file after the site-specificity audit.

#### PATCH-015: Ornith derail Tier-1 fixes — repetition guard, frame re-anchor, tool pinning, prefill continuation, fence-once (`agent/turn_repetition_guard.py`, `agent/conversation_loop.py`, `agent/tool_dispatch_helpers.py`, `agent/tool_executor.py`, `agent/agent_init.py`, `agent/turn_context.py`, `hermes_cli/config.py`)
- **Status:** ON `local`, 5 commits (`e93caa32d`..`64c6f74fb`).
- **What:** Five harness-level fixes for local-model failure classes surfaced by a single 39-message Hermes-darkstar session (`20260729_163715_1b99fd1a`, first cross-node LMS use): (1) **F1** assistant repetition guard — new `agent/turn_repetition_guard.py` (renamed 2026-08-19 from `repetition_guard.py`; see PATCH-018), stateless detector comparing an about-to-be-emitted assistant turn (text, or the tool-call set) against the immediately preceding one; first duplicate injects a corrective line, second consecutive duplicate halts the tool loop; mirrors the kernel agent loop's no-progress guard. (2) **F4** frame re-anchor — after a tool result over 4KB, appends one line naming the live user question so a long retrieval's own discourse can't displace the actual conversation. (3) **F3** tool inventory pinning — re-injects a compact tool-name-only list at the very end of context assembly each turn (recency beats primacy for small models). (4) **F2** truncation continuation switched from an instructional "continue exactly where you left off" nudge (observed producing a restart from the top) to mechanical prefill — resend with the truncated text as a trailing assistant turn, same mechanism this codebase already uses for thinking-only-response recovery. (5) fence-once-per-turn — the `<untrusted_tool_result>` instructional paragraph now fires once per turn (full fence) with a one-word `<untrusted/>` tag on subsequent results, instead of repeating the ~50-token paragraph on every result.
- **New config sections** (profile `config.yaml`): `assistant_repetition_guard`, `frame_reanchor`, `tool_inventory_pinning`, `truncation_continuation` — all default-on with the behavior above.
- **Provenance:** Case study `~/workspaces/cog/.cog/mem/working/2026-07-29-ornith-derail-case-study/CASE-STUDY.md` (2026-07-29). Calibration principle governing the injected-text shape ("every token matters for a small model's calibration") is verbatim-cited in that doc.
- **Test-delta notes:** new `tests/agent/test_repetition_guard.py` (18 tests) + additions to `tests/agent/test_tool_dispatch_helpers.py` (fence-once + frame-reanchor + find-last-user-message, 34 tests). 5 pre-existing tests in `tests/run_agent/test_run_agent.py` updated to assert the new default (prefill) truncation shape and the tool-inventory-pin tolerance — behavior changed by design, tests updated to match. Full `tests/agent/` + `tests/run_agent/` suites re-run before and after: identical pre-existing failure set (missing `python-multipart` dep, network-mocked model-metadata tests, etc.), zero regressions.
- **Upstream refs:** not yet searched — this is a generalizable harness-quality improvement (repetition detection, prefill-based continuation, context recency anchoring) with no obvious site-specific dependency, unlike most of this registry's LOCAL-PERMANENT entries. Plausible upstream audience.
- **NEEDS-WORK gap:** no upstream search done yet for prior art on any of the five; the tool-call-repetition mirror (folded into F1, capped at warn-only to avoid racing `tool_guardrails.py`'s existing, more leniently-tuned halt thresholds) needs its interaction with that existing guardrail called out explicitly in any upstream PR description, since a reviewer familiar with `tool_guardrails.py` will ask why there are now two overlapping mechanisms.
- **Reconciler action:** search upstream for repetition-guard / prefill-continuation / context-recency prior art before filing; if none exists, this is a strong, self-contained PR candidate (no Myrgic-specific coupling).

#### PATCH-016: Model-class gate for the F3/F4 Ornith mitigations (`agent/agent_init.py`)
- **Status:** ON `local`. Amends PATCH-015 (does not revert it).
- **Files:** `agent/agent_init.py` — new module-level helper `_small_model_mitigations_default(agent)`; the `frame_reanchor.enabled` and `tool_inventory_pinning.enabled` reads now use it as their **default** instead of a hardcoded `True`.
- **What:** PATCH-015 shipped F3 (tool-inventory pinning) and F4 (frame re-anchor) **default-on for every model and provider**. Both were written for one observed failure: a small *local* model under heavy retrieval load denying it had a tool it had just used four times. On a frontier hosted model the failure class does not occur, so the mitigation is pure cost — F3 appends a names-only tool list (~600 B, 51 names on this profile) to `api_messages[-1]` **whatever that message's role is**, every turn; F4 appends a restatement of the live user question *inside a tool-result envelope*. The gate returns `False` (mitigations off) for known frontier providers, or for a non-local `base_url` whose model matches a frontier family marker; returns `True` (previous behaviour, unchanged) for local endpoints (`localhost`/`127.0.0.1`/`0.0.0.0`/`::1`) and anything unrecognised. **Explicit config always wins** — this only moves the default.
- **Provenance:** Discord session 2026-08-18. Trailing `[Available tools: ...]` blocks were flagged as suspected prompt injection **twice** before being traced to F3; a subagent's transcript captured F4 reproducing the operator's prompt text nested inside tool output. Both fired repeatedly *during the writing of this patch*, including on the `read_file` of this registry — F3's positional append lands in tool results, which is precisely why undeclared injection is indistinguishable from a hostile one. Analysis: `~/workspaces/cog/.cog/mem/working/2026-08-18-hermes-injection-manifest-spec.cog.md` (8 unrecorded message-array mutation sites, F3/F4 among them).
- **Why not just config:** `agent.tool_inventory_pinning.enabled: false` was already set in profile `cog` — the flags are read **once at agent init**, so a mid-session config edit cannot take effect, and every *other* profile/model on this node still inherits the bad default. The gate fixes the class; the config entry fixed one instance.
- **Test-delta notes:** gate verified against 7 cases (this session's `anthropic`/`claude-opus-5` → off; LM Studio `localhost` e4b → on; darkstar `127.0.0.1` 26b → on; OpenRouter+Claude → off; OpenRouter+small-OSS → on; `openai`/`gpt-5` → off; unset/unknown → on). **7/7 pass.** `py_compile` clean. Full suite not re-run — behaviour is default-only and previous behaviour is preserved for every local/unknown backend.
- **Upstream refs:** not yet searched. Strong upstream candidate — the missing model-class gate is a defect in PATCH-015's own upstream story, and any reviewer of that PR would ask for it.
- **Reconciler action:** file together with PATCH-015, or as a follow-up commit in the same PR.

### COMMENT-DONT-FILE — real bug, but competing upstream PRs already cover the surface

#### PATCH-018: Rename `agent/repetition_guard.py` → `agent/turn_repetition_guard.py` (collision avoidance)
- **Status:** ON `local`. Pure rename + import updates; **no behaviour change**. Never file upstream — this exists only to make our rebase survivable.
- **Files:** `agent/repetition_guard.py` → `agent/turn_repetition_guard.py`; `tests/agent/test_repetition_guard.py` → `tests/agent/test_turn_repetition_guard.py`; import sites in `agent/conversation_loop.py`, `agent/agent_init.py`.
- **What:** both sides created a file at `agent/repetition_guard.py` **after** the 2026-07-04 merge-base, with **disjoint purpose**. Upstream (`b48ab1b4ad`, issue #86581, 95 lines, 2 functions) detects verbatim repetition *within a single truncated fragment* on the `finish_reason=length` continuation path — the incident was one turn emitting 60,698 chars as 31 Discord messages. Ours (PATCH-015 F1, 301 lines) detects near-duplicate repetition *across consecutive assistant turns*, plus tool-call-set repetition, and halts the tool loop. Same filename, unrelated mechanisms; neither should win.
- **Why now:** an add/add conflict on a 301-line file is the worst kind to hit mid-rebase — git offers no useful merge and the temptation is to pick one side. Renaming ahead of time converts it into two files that coexist, and lets upstream's guard land untouched.
- **Test-delta notes:** `tests/agent/test_turn_repetition_guard.py` **19 passed**. Import smoke test through `agent.conversation_loop` + `agent.agent_init` clean. Zero residual references to the old module path outside historical registry prose (PATCH-015's entry annotated with the rename).
- **Upstream refs:** none, and none wanted. If PATCH-015/F1 is ever filed upstream it must be filed under the new name, with #86581's guard explicitly called out as a *neighbour, not a duplicate* — a reviewer who knows #86581 will otherwise assume overlap.
- **Reconciler action:** keep. Re-check at each resync that upstream still owns `agent/repetition_guard.py`; if upstream ever deletes it, this rename can be reverted but there is no reason to.

#### PATCH-017: OAuth sanitizer must not rewrite `hermes-agent` inside paths/URLs (`agent/anthropic_adapter.py`)
- **Status:** ON `local`. Adopts upstream PR #48868's regex **verbatim** so the eventual merge rebases to a no-op.
- **Files:** `agent/anthropic_adapter.py` (`import re`; the `is_oauth` sanitizer loop at ~L2542) + `tests/agent/test_anthropic_adapter.py` (new `TestOAuthSanitizerSlugPreservation`, 6 tests).
- **What:** the OAuth branch ran `identity_text.replace("hermes-agent", "claude-code")` — an unanchored substring replace over the **entire assembled system prompt**, intended only to launder product names past Anthropic's OAuth content classifier. It has no notion that `hermes-agent` is also a directory on disk and a URL host. Replaced with `re.sub(r"(?<![:/\w])hermes-agent(?!\.nousresearch\.com)", ...)`. The **lookbehind** is what protects filesystem paths and URL path segments; the lookahead only protects the docs host.
- **Provenance:** Discord session 2026-08-18. Three independent subagents in `deleg_306885f7` each reported the brief pointed them at `/Users/slowbro/.hermes/claude-code/venv/bin/python` — **a path the parent never wrote**. Isolated with a one-shot canary (`deleg_0c23c148`): subagent told to echo the interpreter string verbatim and run no tools, returned `claude-code`. Reproduced directly against the replace loop. The same corruption is recorded hours earlier in `~/workspaces/cog/.cog/mem/working/2026-08-18-hermes-injection-manifest-spec.cog.md` — it was misfiled as a stale-path authoring error at the time.
- **Defect class:** *transport-layer mutation of agent-authored text.* The corruption happens on the wire, after the text leaves the author, and is invisible to the author — the parent's own reasoning about "what I wrote" is about a string that no longer exists downstream. Fires **only** on the OAuth path, so it is invisible to any test not routed through `build_anthropic_kwargs(is_oauth=True)`. Amplified by delegation specifically: that is where a path in prose becomes a path someone executes. A compliant worker runs `pytest` against a nonexistent interpreter and reports a sincere failure.
- **Test-delta notes:** 6 new tests, all through the real production path (not a local reimplementation of the sanitizer). **Failure baseline captured first:** with the greedy replace restored, 4 of 6 fail (filesystem path, docs host, URL path segment, combined); with the fix, **6/6 pass**. The 2 that pass either way are the "sanitizer is not neutered" guards — bare slug and product names still swap. Full file: 168 passed / 12 failed, **identical pre-existing failure set** before and after (keychain + OAuth-setup-token tests, unrelated).
- **Upstream refs:** issue **#48860 OPEN** (docs-host symptom only) — present in `origin/main:agent/anthropic_adapter.py:2967` today. Three competing PRs open since **2026-06-19**: **#48865** (`\b` variant — would NOT fix the filesystem-path case), **#48868** (`(?<![:/\w])` — does, by accident), **#58659** (same intent, tests). Automated triage already recommends merging #48868, blocked on *a missing trailing newline in the test file*. Related: **#84222** (the `"You are Claude Code"` rewrite makes models call nonexistent `Glob`/`Read`/`Grep`) — same root cause, different symptom.
- **Re-merge-risk:** LOW→NONE. Deliberately byte-identical to #48868's regex; if it merges, this patch disappears into it.
- **Reconciler action:** **do not file a fourth PR.** Comment on #48860 with the canary reproducer, the delegation amplification, and the key correction nobody upstream has stated: *the `.nousresearch.com` lookahead is the wrong anchor for this class* — filesystem paths have no docs host to protect them, so #48865's `\b` variant does not fix them. That is new evidence from a fifth reporter and strengthens the case for #48868 specifically over its two siblings.

#### PATCH-011: `SessionDB._execute_write` None-guard on closed connection (`hermes_state.py`)
- **Status:** ON `local`. Replayed clean at the 2026-07-04 resync (auto-merged, only the PATCHES.md registry itself conflicted). **Live after each gateway/cron-worker restart** (editable install — `hermes_state.py` is imported straight from this repo checkout via `__editable__.hermes_agent-0.18.0.pth` since the 2026-07-04 cutover venv sync; confirmed no separate installed copy shadows it).
- **Files:** `hermes_state.py` (`SessionDB._execute_write`, ~4 lines added before the existing `BEGIN IMMEDIATE` call).
- **What:** `_execute_write` unconditionally called `self._conn.execute("BEGIN IMMEDIATE")` with no guard against `self._conn` having been set to `None` by a concurrent `close()` on the same `SessionDB` instance. Adds an explicit `if self._conn is None: raise RuntimeError(...)` immediately before that call, converting the opaque `AttributeError: 'NoneType' object has no attribute 'execute'` into a typed, self-describing `RuntimeError` that existing broad `except Exception` callers already catch and log.
- **Provenance:** Born 2026-07-04 09:20:17 EDT (`2877f37e7`). Confirmed live race during the 2026-07-04 Hermes triage session: darkstar `errors.log:2652-2660`, job `hermes-upstream-reconciler`, session `cron_b02d262b332d_20260703_060318`, failure timestamp 2026-07-03 06:21:47 — `cron/scheduler.py`'s inactivity-timeout branch tore down `_session_db.close()`/`agent.close()` in its `finally:` while the timed-out job's worker thread was still executing. TRIAGE.md issue 2 (fallback-path session-persistence loss, SEV HIGH). Root-cause companion **PATCH-012** landed same day (PATCH-011 typed the failure; PATCH-012 stops the message loss at its root).
- **Test-delta notes:** full `tests/test_hermes_state.py` (303 tests) passes; guard confirmed live via direct smoke test (closed-connection write raises typed `RuntimeError`, not `AttributeError`).
- **Upstream refs:** Verified live in `upstream/main:hermes_state.py:1129` — `_execute_write` still calls `self._conn.execute("BEGIN IMMEDIATE")` with no None-guard; the bug is still exactly reproducible upstream today. But **two independent contributors are already mid-flight on the identical surface**, not previously cross-linked by the original dossier: **PR #46479** OPEN ("guard SessionDB operations after close") — adds a `_check_open()` guard called at the top of *every* public accessor/mutator (broader than this patch's single `_execute_write` guard), raises the same `RuntimeError` type, makes `close()` idempotent. **PR #54706** OPEN ("re-open SessionDB conn when a concurrent close() nulled it") — materially different strategy: instead of failing typed, transparently **re-opens** the nulled connection so the write silently succeeds — closer in spirit to PATCH-012's message-preservation goal than to this patch's typed-error approach. Parent issue **#54755 OPEN** (also an anchor for PATCH-012).
- **Re-merge-risk:** LOW — additive guard on an already-exceptional path; no behavior change on the non-null-`_conn` path (the overwhelming majority of calls).
- **Reconciler action:** **comment on #54755 (or on #46479/#54706 directly), citing this patch's narrower single-function guard as an alternative — do not file a third independent PR.** Two other contributors already cover broader or materially different variants of the identical fix; a fresh PR risks fragmenting review. Watch `hermes_state.py`'s `SessionDB._execute_write`/`close()` for upstream changes to connection-lifecycle handling; drop this local patch if upstream adds an equivalent guard or a lock-based fix that supersedes it (either #46479 or #54706 merging would qualify).
- **Deferred (not applied, needs operator review):** a second, root-cause diff was proposed against `cron/scheduler.py` (skip the `_session_db.close()`/`agent.close()` teardown in the `finally:` block when the inactivity-timeout branch fired and the future is not yet `.done()`) — flagged as "a sketch, not a verified patch," noting fd/subprocess-lifetime risk. Not applied; refined and ratified as **PATCH-012** instead (the `add_done_callback` ownership-transfer approach with a hard-cap bound).

---

## LOCAL-PERMANENT

Site-specific by construction. No generic upstream audience exists for these; they are not
candidates for filing and are not expected to ever leave this registry except by direct
supersession.

#### PATCH-002: Untracked test file / skill dir
- **Why local:** working-tree residue (a plugin test file and a devops skill directory) that never got committed or relocated — pure git-hygiene risk, not a feature.
- **Status:** PARTIALLY RESOLVED — `test_memory_tool_eviction.py` committed with PATCH-001 (2026-06-10). Still untracked: `tests/plugins/test_skill_relevance_gate.py`, `skills/devops/kanban-closed-loop-supervisor/`.
- **Provenance:** Not recoverable from git by definition (never committed). First documented in the external pre-resync registry, created 2026-06-03, already listed as pre-existing untracked items at that point. `~/.hermes/plugins/hermes-achievements/` state files date to 2026-05-30, giving a rough lower bound for how long untracked material had already accumulated before anyone inventoried it. Standing item, unresolved across at least three audit passes (2026-06-03, 2026-07-04 resync, this 2026-07-05 pass).
- **Risk:** LOW — at risk from `git reset --hard`/`clean -x`, not from rebase churn (nothing to conflict).
- **Path to safety:** commit on `local`, or relocate to `~/.hermes/plugins/skill_relevance_gate/tests/`.

#### PATCH-004: Myrgic overlay header in `AGENTS.md`
- **Why local:** a fork-identity banner naming Myrgic/the local branch model has no upstream target by definition — this is infrastructure for *this checkout*, not a feature upstream could adopt.
- **Status:** ON `local` (this commit), unchanged since creation.
- **Files:** `AGENTS.md` (prepended block, delimited by `MYRGIC-LOCAL-OVERLAY:START/END`)
- **What:** Prepends a "READ FIRST" overlay declaring this a vendored checkout (not Myrgic source), pointing agents at this file + the branch model, ahead of upstream's contributor guide. Solves the "agent treats the fork as its own source" failure mode.
- **Provenance:** Born 2026-06-03 10:06:32 EDT (`efe0cffd3`) — the same commit that created this in-repo registry itself. No prior incident; infrastructure-first work.
- **Risk:** LOW — additive, prepended above upstream content; rebase conflicts only if upstream rewrites the first lines of `AGENTS.md` (re-apply the delimited block). Kept-clean through both the 2026-06-10 and 2026-07-04 rebases with zero conflicts.

#### EXT-001-Gate1-note: `oauth_billing_gate` plugin, Gate 1 half
- **Why local:** the plugin's Gate 1 tool-prefix forcing is now upstream-redundant-but-harmless — see the full EXT-001 entry below for the shared plugin, and ABSORBED-BY-UPSTREAM for the Gate 1 commits it duplicates. Documented separately here because Gate 1 and Gate 2 have different tracks (Gate 1 = local-permanent-but-simplifiable, Gate 2 = deliberate-divergence, tracked under PATCH-013's shared logic).
- **Status:** Gate 1 (`_MCP_TOOL_PREFIX` forcing + unconditional tool-name re-walking) still runs on top of an upstream body that already does the identical fix correctly natively. No functional break. **TODO(simplification, not yet done):** drop the redundant Gate 1 forcing now that upstream ships `3d3786929`+`b70a4e753` natively — flagged in RESYNC-LEDGER.md, still open.
- **Path to safety:** N/A — this is a "can be simplified away," not a "needs filing." See EXT-001's main entry for the plugin as a whole.

#### EXT-002: `mod3_session` plugin
- **Why local:** wires Hermes into the Mod³ TTS/voice-dashboard system's session/seat lifecycle, SSE subscriber, and kernel-first session authority — Mod³ is Myrgic-local infrastructure with no upstream analog.
- **Provenance:** Predates the 2026-06-03 registry — already listed, fully formed, "upstream-safe: YES," no creation date recorded. No incident or task record surfaced; reads as planned Mod³ infrastructure build-out.
- **Path to safety:** N/A — user-space plugin, survives any reset by construction.

#### EXT-003: `mod3_voice` plugin
- **Why local:** Discord voice-channel pipeline (SSE, WebSocket audio, VAD barge-in, `voice_output` tool) via Mod³ — same Mod³ dependency as EXT-002, no upstream analog.
- **Provenance:** Predates the 2026-06-03 registry. Extensive design-journal material exists in `~/.hermes/journals/mod3-discord-voice-dreamer.md` (active through at least 2026-06-01) suggesting iterative design rather than incident-triggered work, but that's a design log, not a birth record.
- **Path to safety:** N/A — user-space plugin.

#### EXT-004: `bw_bridge` plugin
- **Why local:** Vaultwarden secret bridge (bootstrap/deposit/resolve + `secret:` URI projection) — implementation is tied to the operator's specific secret-manager choice; no generic secret-backend interface exists in this repo to compare against. The underlying *pattern* (secret-manager bridge) could inform a future generalized interface, but the implementation itself is not portable as-is.
- **Provenance:** Predates the 2026-06-03 registry. No incident record; reads as planned secrets-management infrastructure.
- **Live touch:** the 2026-07-04 triage session's Vaultwarden cert-trust cascade (Addendum 2) modified a companion file (`bw_bridge/secret_resolver.py`'s `unlock_session()`, both `bw unlock` calls switched to `_bw_env()`) — a live fix to the implementation, not a re-founding; core purpose unchanged.
- **Path to safety:** N/A — user-space plugin.

#### EXT-007: `auto-voice-reply` hook
- **Why local:** triggers automatic voice replies, depends on the Mod³ voice stack (EXT-002/003) — same site-specificity class.
- **Provenance:** Predates the 2026-06-03 registry. No incident record recovered.
- **Path to safety:** N/A — user-space hook.

#### EXT-008: `bw-eclipse-key` hook
- **Why local:** injects the Eclipse (home-desktop LM Studio node) API key from Vaultwarden at gateway startup for the `cog` profile — names a specific machine, secret store, and profile by construction.
- **Provenance:** Predates the 2026-06-03 registry.
- **Live touch:** the 2026-07-04 triage session's Vaultwarden cert-trust cascade (Addendum 2) found and fixed a masked-layer bug the same day — `PROVIDER_KEYS` in `handler.py:42` still targeted old key names `cogos-eclipse`/`eclipse`, but the live config key had drifted to `eclipse-lms`. Fixed, backup kept. Result: "first successful completion of this hook ever" per FIXES-LEDGER.md Addendum 2 — this hook had likely never actually worked correctly end-to-end before 2026-07-04, despite existing since before the registry.
- **Path to safety:** N/A — user-space hook.

#### EXT-009: `mod3-voice-bootstrap` hook
- **Why local:** bootstraps the Mod³ voice pipeline at gateway startup — same Mod³ dependency family as EXT-002/003/007.
- **Provenance:** Predates the 2026-06-03 registry. No incident record recovered.
- **Path to safety:** N/A — user-space hook.

#### EXT-010: `claude-token-refresh` actuator
- **Why local:** macOS Keychain + local Claude Code CLI binary path, outside the repo by construction. The underlying "delegate refresh to the actual token owner via a headless CLI call" *pattern* is worth folding into PATCH-007/008's eventual PR narrative as a reference implementation, not shipping the script itself.
- **What:** Single-flight headless `claude -p` actuator that refreshes the CC-owned Keychain OAuth token on demand: fast-path (fresh token already) exits in under 1s, forced-stale path runs `claude -p` and exits in roughly 9s.
- **Provenance:** Created alongside PATCH-007, 2026-06-03. Same incident as PATCH-007 — once Hermes was made strictly read-only against the CC-owned Keychain token, something still needed to trigger a refresh when even the keychain copy went stale.
- **Path to safety:** N/A — user-space actuator, delegate target cited by both PATCH-007 and PATCH-008's fallback paths, unchanged in mechanism since creation.

---

## DELIBERATE-DIVERGENCE

Cases where upstream has an explicit, known stance — either shipped a lighter/different fix that
this repo's maintainer (operator) chose not to adopt, or has no equivalent by design — and this
repo knowingly diverges. Distinct from LOCAL-PERMANENT: these *could* in principle track upstream
but a deliberate decision keeps them off that path (for now).

#### PATCH-001: Progressive memory eviction (`tools/memory_tool.py`)
- **Status:** LIVE on `local` (landed 2026-06-10, `e0388eee5`, "feat(memory): silent Tier 1→Tier 3 eviction at 75% capacity (MEMDUMP-001)"). Reimplemented against the eviction test suite; the original draft in `stash@{0}` is redundant — verify and drop.
- **Files:** `tools/memory_tool.py` (+175), `tests/tools/test_memory_tool.py` (legacy limit test repointed at the preserved hard-error path), `tests/tools/test_memory_tool_eviction.py` (now tracked).
- **What:** Silent Tier 1→Tier 3 eviction at >75% capacity; evicted entries → CogOS cogdocs (substrate overflow dir, fallback `~/.hermes/memories/hermes-overflow`); Tier 2 pointer index; `scan_tier2_index_for_hot_entries()` read-side helper. An entry larger than the whole limit still stays a hard error.
- **Provenance:** Task assigned 2026-05-25 22:15 EDT (kanban `t_4f9f0fc7`, session `20260525_175814_2e2a1c`, darkstar profile), against a pre-written architecture cogdoc (`cog://mem/semantic/architecture/progressive-memory-architecture`) — planned substrate work, not an incident scramble. Landed on `local` 2026-06-10 19:27 EDT. Closes incident MEMDUMP-001 — the memory tool's `add()` hard-failed with a user-visible error once Tier 1 hit capacity; operator decision 2026-06-10 to give it a Tier1→Tier3 degrade path instead.
- **Upstream's stance:** Issue **#23378** ("Bounded memory overflow does not trigger automatic consolidation/retry") describes the exact hard-fail-on-overflow premise this patch fixes. **CLOSED as COMPLETED** 2026-06-08 via merged **PR #41755** / commit `86c537d20` ("instruct in-turn consolidation + retry on overflow"). Verified live in `upstream/main:tools/memory_tool.py` — `add()` **still returns `success: False`** on overflow (the hard-fail contract is unchanged); what changed is the error message now explicitly instructs the model to consolidate-and-retry *in the same turn*. The maintainer's closing comment on #23378 explicitly **considered and rejected** the heavier synchronous-cleanup approach this patch takes, calling it "heavier than warranted" and opting for sharpened in-loop guidance instead.
- **Divergence rationale:** this patch's silent Tier1→Tier3 eviction is a materially different contract — never returns `success: False`, evicts to a CogOS cogdoc instead of asking the model to consolidate. Not "unfiled and waiting," but "at odds with a deliberate upstream design decision." A future PR would need to argue why silent-CogOS-eviction beats the maintainer's already-shipped lighter fix, not just propose the missing feature.
- **Test-delta notes:** two sibling tests (`test_add_overflow_degrades_after_cap`, `test_batch_frees_room_for_otherwise_overflowing_add`) were never updated for the new silent-eviction contract during a re-merge; marked `xfail`-with-reason on 2026-07-04 (`55b89aebc`, during the resync fix-forward pass — the mechanical validation gate failed 5 tests, 2 of which were these) rather than fixed or deleted, following the no-stubs discipline: document the contract change, don't erase the historical test. Correctly documented as contract-tracking, not defects.
- **Re-merge-risk:** carried forward clean (auto-merge) through both the 2026-06-10 and 2026-07-04 (3,093-commit) rebases.
- **Reconciler action:** none pending — deliberately not filed. Revisit only if the CogOS-cogdoc overflow target is ever generalized into something a stock install could use, which would change the shareability calculus entirely.

#### PATCH-001 test-deltas (xfails) — `tests/tools/test_memory_tool.py`
- **Provenance:** 2026-07-04 12:06:48 EDT (`55b89aebc`), during the resync fix-forward pass, same pass as the PATCH-008 keychain xfail and the PATCH-010 mock-shape fix.
- **Why xfail, not deleted:** the gate is mechanical (any regressing test = RED, no cutover). Rather than silently deleting or skipping the now-invalid assertions, they were marked `xfail` with the reason cited by name (PATCH-001) — document the contract change, don't erase the historical test.
- **Status:** standing, no action needed — this is documented local test debt tracking PATCH-001's divergence, not a functional defect.

#### EXT-001-Gate2 (shared logic with PATCH-013): `oauth_billing_gate` plugin, Gate 2 half
- **Why deliberate-divergence:** independent plugin-level copy of PATCH-013's Gate 2 relocation logic — a belt-and-suspenders layer built specifically because the underlying fix had already been lost twice to branch/reset operations predating any source-level patch. A plugin outside the tracked repo survives any `git reset`/`checkout upstream/main` that would clobber an in-repo commit. See PATCH-013 (NEEDS-WORK-TO-FILE above) for the full upstream landscape this logic shares.
- **Provenance:** RCA dated 2026-05-29 (`cog://mem/semantic/research/2026-05-29-anthropic-oauth-dual-gate-definitive`); plugin created on or before 2026-06-03. A same-day-of-creation fix cycle recorded 2026-06-05: ~17:08 an edit changed built-ins to `bare→bare` (to fix the `mcp__patch` round-trip) which reopened Gate 2's tool-array face → 400 storm from 17:23; fixed same day (restored `bare→mcp__` AND added the deterministic inbound reversal).
- **Status:** this stays as the plugin's whole reason to exist until PATCH-013 lands upstream and is confirmed to survive a reset.
- **Standing operational gap (unresolved across both 2026-07-04 and 2026-07-05 audits):** `PluginManager.discover_and_load()` under `HERMES_HOME=~/.hermes/profiles/cog` never finds this plugin because `~/.hermes/profiles/cog/plugins/` doesn't exist as a directory — the plugin only lives at base `~/.hermes/plugins/`. Low operational risk today because PATCH-013 (the in-repo source patch) covers Gate 2 regardless of whether the plugin loads under `cog`. **TODO(hygiene): create `~/.hermes/profiles/cog/plugins/` or otherwise fix plugin discovery for the cog profile.**

---

## ABSORBED-BY-UPSTREAM

Historical. Patches whose upstream equivalent landed independently and content-verified identical
— the patch dropped out at rebase (commit went empty). Kept here as a record, not an action item.

#### PATCH-003 → `abc3662bf`: launchd `/restart` detection (`gateway/slash_commands.py`)
- **Dropped commits:** `9d8b673d7`, `ee023c33c`, `10e01c310`, `b3b1f2401` (4 commits, 2026-07-04 resync).
- **Upstream equivalent:** `abc3662bf` — "fix(gateway): detect launchd in /restart service-manager probe (#43475)".
- **Verification:** content-compared, not just an empty cherry-pick diff — same `XPC_SERVICE_NAME not in ("", "0")` guard, same exclusion-of-literal-`"0"` reasoning, explicitly "Fixes #43475" (the same issue local PR #43888 targeted). The pin test (`tests/gateway/test_restart_service_detection.py`) also came up empty because upstream ships the identical 4 test cases verbatim.
- **History note (do not re-flag without checking this):** was previously marked superseded 2026-06-10 and that call was **wrong** — upstream's `darwin` check at the time only lived at the exit-code site (`run.py`), not the `/restart` handler; re-applied same night after a live 25-min gateway outage. This time (2026-07-04) the check was traced against the handler itself (`_under_service` in `slash_commands.py`) and confirmed genuinely identical.
- **TODO(closeable): PR #43888** (our original filing for this fix) is closeable now that `abc3662bf` covers it — close/reference on the next PR sweep.
- **Re-derive if upstream regresses:** `_under_service = bool(INVOCATION_ID) or XPC_SERVICE_NAME not in ("", "0")`.

#### Gate 1 of `da816abe5` → `3d3786929` + `b70a4e753`: `_MCP_TOOL_PREFIX` single-underscore→double-underscore
- **Dropped commit:** `da816abe5` (Gate-1 half only; Gate-2 half survived, renumbered PATCH-013 — see DELIBERATE-DIVERGENCE).
- **Upstream equivalent:** `3d3786929` (2026-06-15, fixes #46675) + `b70a4e753` (2026-06-17, MCP-server tool-name normalization).
- **Verification:** same root cause, same constant change, same skip-guard logic — upstream's `build_anthropic_kwargs` now has its own `_to_oauth_wire_name()` closure doing the identical bare→`mcp__`/`mcp_`→`mcp__` promotion. Independently reconfirmed present in `git log --all` on the resync clone during the 2026-07-05 xref pass.
- **Note:** EXT-001's plugin still forces `_MCP_TOOL_PREFIX = ""` and re-walks tool names unconditionally on top of an upstream body that now does this correctly natively — see EXT-001-Gate1-note under LOCAL-PERMANENT for the not-yet-done simplification.

---

## Files to Watch (upstream changes here may break a patch or plugin)

| File | Why | Affected |
|------|-----|----------|
| `tools/memory_tool.py` | PATCH-001 | PATCH-001 |
| `gateway/run.py` | startup/shutdown hooks; exit-code site `via_service` logic pairs with the (absorbed) PATCH-003 | EXT-002, EXT-003, PATCH-003 (historical) |
| `gateway/slash_commands.py` | `_handle_restart_command` service-manager detection | PATCH-003 (historical) |
| `AGENTS.md` | First lines (overlay anchor) | PATCH-004 |
| `tools/tts_tool.py` | TTS output path naming | PATCH-006 |
| `agent/anthropic_adapter.py` | oauth_billing_gate wraps `build_anthropic_kwargs` + forces `_MCP_TOOL_PREFIX=""`; `_refresh_oauth_token`/`_write_claude_code_credentials` read-only | EXT-001, PATCH-008 |
| `agent/agent_runtime_helpers.py` | oauth_billing_gate wraps `repair_tool_call` (inbound `mcp__`→registered-name reversal) | EXT-001 |
| `agent/transports/anthropic.py` | oauth_billing_gate wraps `normalize_response` (inbound `mcp__` reversal) | EXT-001 |
| `agent/credential_pool.py` | `_refresh_entry` claude_code read-only branch | PATCH-007, EXT-010 |
| `ui-tui/src/app/slash/commands/core.ts` | `/steer` carries `aliases: ['s']` | PATCH-009 |
| `gateway/platforms/base.py` | `cancel_background_tasks` grace window | PATCH-010 |
| `hermes_state.py` | `SessionDB._execute_write` None-guard on closed connection | PATCH-011 |
| `cron/scheduler.py` | `_run_job_impl` timeout branch + `finally:` teardown → `_teardown_cron_job_resources`; watch for further insertion-point collisions near `run_job` (upstream's `_guard_job_credential_exfil` collided here once already) | PATCH-012 |
| `agent/anthropic_adapter.py` (again) | `build_anthropic_kwargs`'s `is_oauth` branch system-field handling; `convert_messages_to_anthropic`'s message-history normalization runs BEFORE this patch's relocation call | PATCH-013 |
| `gateway/platforms/discord/adapter.py` | Discord voice | EXT-003 |
| `hermes_cli/plugins.py` | Plugin loading | all EXT-* |
| `agent/background_review.py` | `IdleReviewScheduler` + `_idle_trigger_config` + turn-stamp helpers (idle-trigger section at end of module) | PATCH-014 |
| `run_agent.py` (again) | `_spawn_background_review` dispatcher → `_spawn_background_review_now`; `run_conversation` forwarder turn stamps; `close()`/`release_clients()` scheduler shutdown | PATCH-014 |
| `hermes_cli/config.py` | `auxiliary.background_review.{idle_trigger_seconds,coalesce}` defaults | PATCH-014 |

---

## User-Space Extensions (safe — outside this repo, survive any reset)

EXT-001 `oauth_billing_gate` (DELIBERATE-DIVERGENCE for Gate 2, LOCAL-PERMANENT-simplifiable for Gate 1) · EXT-002 `mod3_session` (LOCAL-PERMANENT) · EXT-003 `mod3_voice` (LOCAL-PERMANENT) · EXT-004 `bw_bridge` (LOCAL-PERMANENT) · EXT-005 `skill_relevance_gate` (UPSTREAM-TRACK, NEEDS-WORK-TO-FILE) · EXT-006 `hermes-achievements` — **vendored-stock, not a local patch: removed from tracking, see note below** · EXT-007 `auto-voice-reply` hook (LOCAL-PERMANENT) · EXT-008 `bw-eclipse-key` hook (LOCAL-PERMANENT) · EXT-009 `mod3-voice-bootstrap` hook (LOCAL-PERMANENT) · EXT-010 `claude-token-refresh` (LOCAL-PERMANENT, actuator for PATCH-007/008 — single-flight headless `claude -p` to refresh the CC-owned keychain token on demand).

Full detail: `~/.hermes/journals/hermes-patch-registry.md`.

**EXT-006 note (2026-07-05):** `~/.hermes/plugins/hermes-achievements/` was carried in prior registries as an "unknown purpose" local plugin. Direct byte-for-byte diff against `upstream/main:plugins/hermes-achievements/` (HEAD `605727e3b`, 2026-07-05) confirms all 12 tracked files — `LICENSE`, `README.md`, `dashboard/dist/{index.js,style.css}`, `dashboard/manifest.json`, `dashboard/plugin_api.py`, all 3 `docs/*.md` specs, both `docs/assets/*.png`, `tests/test_achievement_engine.py` — are **byte-identical** to upstream (matching SHA-256 per file). `plugins/hermes-achievements/` is a genuine, actively-maintained upstream feature (tracked via upstream issue #18472, a +1907-line feature with an open backlog and bug reports #28661/#26927/#36485/#35116/#29380). The local copy is vendored stock, installed via the normal plugin mechanism — not a Myrgic addition. Removed from local-patch shareability tracking entirely (already shared by construction, nothing to file or diverge from). The only local-machine-specific content is *runtime state* (`state.json`, `scan_snapshot.json`, `scan_checkpoint.json`, dated 2026-05-30) which is data, not source, and was never a candidate for this registry.

---

## History

| Date | Action | Patch | Notes |
|------|--------|-------|-------|
| 2026-06-03 | Created in-repo registry | All | Canonical patch list moved into repo on `local` |
| 2026-06-03 | Cherry-picked | PATCH-003 | launchd `/restart` fix from open PR #33393 (`f54c2854e`) |
| 2026-06-03 | Added | PATCH-007 + EXT-010 | Claude Code OAuth credential read-only: stop Hermes POSTing CC's single-use refresh token (no race), delegate refresh to the owner via headless `claude -p` actuator. Root-cause fix for post-`/login` 401 bursts. |
| 2026-06-04 | Added | PATCH-008 | Completes PATCH-007: the uncovered `anthropic_adapter` resolve path (`_refresh_oauth_token`) still POSTed + wrote `~/.claude/.credentials.json`. Made it read-only (actuator delegate + keychain re-read); `_write_claude_code_credentials` → no-op. Residual file-poisoner after the CogOS kernel WriteBack fix (myrgic/cogos#363/#364). |
| 2026-06-03 | Added | PATCH-004 | AGENTS.md Myrgic overlay header |
| 2026-06-03 | Added | PATCH-006 | TTS concurrent output path collision (microsecond timestamp). PATCH-005 (voice chunk pipelining) attempted same session, scrapped — did not fix the gap. |
| 2026-06-04 | Added | PATCH-009 | `/s` alias for the TUI `/steer` slash command (`ui-tui/src/app/slash/commands/core.ts`). One-line `aliases: ['s']`. Parity tests + build verified. |
| 2026-06-10 | Landed | PATCH-001 | Eviction implemented against the test suite and committed to `local` (closes MEMDUMP-001 / t_4f9f0fc7; operator decision 2026-06-10). `stash@{0}` draft now redundant — verify and drop. |
| 2026-06-10 | Superseded | PATCH-003 | Dropped at rebase onto `origin/main` (upstream restart path handles launchd via `darwin` platform check; XPC probe + PR #33393 obsolete). Verify `/restart` after next gateway restart. |
| 2026-06-10 | Rebased | All | `local` stack rebased onto `origin/main` (was 272 behind). 11 commits survive; only conflict was PATCH-003 (dropped). Memory + auth/oauth suites green on the rebased base. |
| 2026-06-10 | Added | PATCH-010 | /restart ack was cancelled mid-send by teardown (50ms handler-to-cancel vs one HTTPS round trip); `cancel_background_tasks` gains a restart-scoped 2s grace. Found while verifying PATCH-003 (operator saw only the post-boot confirmation, never the ack). |
| 2026-06-10 | Re-applied | PATCH-003 | "Superseded" call was wrong — upstream's launchd handling lives only at the exit-code site, never reached because the `/restart` handler (`gateway/slash_commands.py`) doesn't detect launchd. Regression bit live at 20:46 EDT (cog gateway dead after Telegram `/restart`). Re-applied in the handler with `XPC_SERVICE_NAME not in ("", "0")` (interactive shells inherit `=0`). Upstream PR candidate. |
| 2026-07-04 | Added | PATCH-011 | `SessionDB._execute_write` None-guard: closed-connection race (cron inactivity-timeout teardown racing an orphaned worker thread's `append_message`) converted from opaque `AttributeError` to typed `RuntimeError`. Root-cause `cron/scheduler.py` teardown-ordering fix proposed but deferred — needs operator review (fd/subprocess-lifetime tradeoff), see PATCH-011 entry. Hermes triage 2026-07-04, TRIAGE.md issue 2. |
| 2026-07-04 | Added | PATCH-012 | Root-cause fix for the PATCH-011 race: cron job teardown deferred to worker completion via `add_done_callback`, bounded by `HERMES_CRON_TEARDOWN_HARDCAP`; subprocess kill moved into the timeout branch. Upstream PR NousResearch/hermes-agent#58311 filed same session. |
| 2026-07-04 | Rebased | All | `local` (21 commits, `97b15c48c`) rebased onto real `origin/main` tip `86a0c5553` (was 3,093 commits behind — 24 days without a sync run). Isolated clone/worktree, never touched the live `~/.hermes/hermes-agent` checkout. PATCH-003 (+ its pin test) genuinely dropped out empty (upstream `abc3662bf` landed the identical fix). `da816abe5`'s Gate 1 half (mcp_→mcp__) also dropped — upstreamed independently (`3d3786929`/`b70a4e753`); its Gate 2 half re-applied as newly-numbered **PATCH-013**. Real conflicts on PATCH-010 (`cancel_background_tasks`/`_stop_impl` — upstream rewrote the drain mechanism and refactored teardown into `_bounded_adapter_teardown`; `grace_seconds` re-threaded through the new surface), PATCH-007 (`credential_pool._refresh_entry` — upstream added an `openai-codex` serialization branch; claude_code gate reordered ahead of it), PATCH-008 (`_refresh_oauth_token` — upstream's own `1dde7e2f2` race-mitigation fast path kept, POST-fallback re-routed through the EXT-010 actuator; highest-risk hunk of the resync), and PATCH-012 (`cron/scheduler.py` — pure positional collision with upstream's new `_guard_job_credential_exfil`, resolved as sibling functions). All patches verified live in an isolated venv (`uv pip install -e ".[dev]"` in the worktree): PATCH-001 eviction (16/16), PATCH-010 grace window (3/3), PATCH-007 pool (101/101), PATCH-008 read-only contract (7/7 + 0 new failures vs the pre-existing 14-failure adapter-test baseline, confirmed identical on plain `origin/main`), PATCH-011 guard (303/303 + live smoke), PATCH-012 teardown (repro harness 4/4 scenarios + 618/618 cron tests), PATCH-013 Gate 2 (functional end-to-end + EXT-001 attach-point re-verification, all 4 points resolve and the plugin patches successfully). Full report: `~/workspaces/cog/.cog/mem/working/2026-07-04-hermes-resync/rebase-report.md`. Cutover to the production checkout gated on a separate mechanical all-green pass — not run in this session. |
| 2026-07-04 | Fix-forward | PATCH-001, -008, -010 | Post-validation remediation of the 5 re-merge-gap test regressions that gated the first cutover attempt: PATCH-010's `grace_seconds` forwarded only when non-default (`8ce044176`, production-code fix — upstream's zero-arg teardown mocks no longer break); the 2 PATCH-001 silent-eviction sibling tests (`55b89aebc`) and the 1 stale pre-PATCH-008 direct-POST keychain fallback test (`8ae25def2`) marked as documented local xfail (they assert superseded local contracts, not defects). Validation re-run → ALL-GREEN (`validation.md`). |
| 2026-07-04 | Cutover | All | Live checkout fast-forwarded: `local` `97b15c48c` → `8ae25def2` (rebased stack + 3 fix-forward commits); `main` advanced to upstream base `86a0c5553`. Venv synced in place: hermes-agent editable metadata `0.16.0` → `0.18.0` (finder regenerated, picks up new top-level modules e.g. `mini_swe_runner`), `aiohttp` `3.13.4` → `3.14.1` (upstream cites CVE-2026-34993 RCE), `setuptools` `82.0.1` → `81.0.0` (upstream torch `>=2.11` cap), `certifi` bump — 4 packages changed, nothing removed. Both gateways restarted via launchctl and verified: Discord connects (Cog#5887, Darkstar#0987) within ~6s, `bw-eclipse-key` hook patched the vault key, cron ticker heartbeats fresh on both profiles, CLI smoke turn persisted a session row with non-zero token counts, zero new tracebacks. Rollback branch `local-pre-resync-2026-07-04` retained at `97b15c48c` + pre-sync pip freeze. Report: `~/workspaces/cog/.cog/mem/working/2026-07-04-hermes-resync/cutover-report.md`. |
| 2026-07-05 | Added | PATCH-014 | Idle-triggered background self-improvement review: `auxiliary.background_review.idle_trigger_seconds` (default 0 = unchanged) defers the post-turn review fork until the session is idle, debounced on new messages, coalescing N turns into one review — so it never queues ahead of live user messages on a single-lane provider. Operator directive + cog incident session `20260704_230542`. Upstream PR drafted, held. |
| 2026-07-05 | Refactored | All | Registry reorganized from a single "at-risk patches" list into vendoring-discipline sections (UPSTREAM-TRACK with FILED-OPEN/READY-TO-FILE/NEEDS-WORK-TO-FILE/COMMENT-DONT-FILE sub-states, LOCAL-PERMANENT, DELIBERATE-DIVERGENCE, ABSORBED-BY-UPSTREAM), each entry enriched with a provenance block, upstream refs, test-delta notes, and re-merge-risk line, per the 2026-07-05 shareability audit (`~/workspaces/cog/.cog/mem/working/2026-07-05-patch-shareability/`). **EXT-006 (`hermes-achievements`) removed from local-patch tracking** — byte-for-byte diff against `upstream/main` confirmed it is vendored stock, not a Myrgic addition (see EXT-006 note above). No patch IDs renumbered; PATCH-011 (previously embedded inline) broken into its own full section reflecting a COMMENT-DONT-FILE state newly surfaced by the audit (PRs #46479/#54706 already cover its surface). |
| 2026-07-29 | Added | PATCH-015 | Ornith derail Tier-1 fixes (5 commits): assistant repetition guard (F1, new `agent/turn_repetition_guard.py`, renamed 2026-08-19), frame re-anchor after large tool results (F4), tool-inventory pinning at end of context (F3), truncation continuation switched from instructional nudge to mechanical prefill (F2), and untrusted-tool-result fence reduced to once-per-turn (fix #5). Case study: `~/workspaces/cog/.cog/mem/working/2026-07-29-ornith-derail-case-study/CASE-STUDY.md`. New config sections `assistant_repetition_guard`/`frame_reanchor`/`tool_inventory_pinning`/`truncation_continuation`, all default-on. Verified zero regressions against full `tests/agent/`+`tests/run_agent/` suites (identical pre-existing failure set before/after). Not yet searched for upstream prior art. |
